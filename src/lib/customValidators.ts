import { CustomValidatorFunction, CustomValidatorReturnValue } from './validators';

// export const validateTrack: CustomValidatorFunction<Song | null> =  (value: any) => {
//   const song = songFromFid(value.toString());
//   return {
//     success: !!song,
//     value: song
//   };
// };